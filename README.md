<p>After installing docker and docker-compose run to build the application and run it</p>

<pre>docker-compose up --build</pre>

<p>if you have already built the application, then</p>
<pre>docker-compose up</pre>